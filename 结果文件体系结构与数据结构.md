# pptx-wzq 结果文件体系结构与数据结构

> 版本：pptx-wzq 1.5.x（可视逻辑块流程 text → formula → blocks → related → author → blocks_json）  
> 日期：2026-08-17 · 作者：吴振谦  
> 本文档描述 `pptx-paser` 全流程产出的**结果目录体系结构**与**每个结构文件的内部数据结构**。

---

## 1. 结果目录体系结构

```
结果目录/                          ← -o 指定的输出目录
├─ images/                         顶层图片集（统一目录）= 仅「可视逻辑块渲染图」
│   ├─ slide_01_blk_01.png         可视逻辑块渲染图（blocks 步骤裁剪自整页渲染）
│   └─ ...                         命名：slide_{页:02d}_{blk}_{块id}.png
├─ sources/                        可视逻辑区的资源内容（矢量/Visio 可编辑资产）
│   └─ slide_05_blk_02.vsdx        命名：slide_{页:02d}_{块id}.{ext}（块持有）
├─ <名>_visual_blocks.json         核心产物：可视逻辑块全栈解析（pptx_multimodal_slide_v2.0）
├─ <名>_visualBlock_text_binding.json  可视逻辑区 ↔ 文本 图文关联关系（块↔文本）
├─ <名>_captions.md                可视逻辑块解读（每块一条，对应 IMG 编号）
├─ <名>_textbook.md                教材文案（每页一节；原文超 300 字直出）
├─ <名>_related_filter.json        图文相关性过滤审计（被删块 + 原因）
├─ state.json                      断点续传状态机（步骤 status：pending/running/partial/done/failed）
├─ pipeline.log                    运行日志（每步开始/完成/失败时间戳）
└─ 过程文件/                        全部中间产物（溯源/二次加工用）
    ├─ blocks/                     步骤3 blocks：atomic_objects.json（几何地基）、
    │                              by_page/（媒体/矢量源工作副本）、images/（块渲染图工作副本）、
    │                              sources/（块矢量资源工作副本）、
    │                              <名>_visualBlock_text_binding.json（工作副本）、
    │                              <名>_visual_blocks.json（工作副本）
    ├─ text/                       步骤1：<名>_texts.md + <名>_text_entries.json
    ├─ formula/                    步骤2：<名>_formulas.md（+ <名>_filtered_entries.json）
    └─ <名>_textbook.md            步骤5 工作副本
```

**数据流**：步骤产出 → 下一步输入。  
`text` 的 `texts.md` → blocks/related/author；`formula` 的 `formulas.md` → blocks/author；  
`blocks` 步骤**自举**准备原子对象（`atomic_objects.json` + `by_page/`，原 `img` 步骤已并入），  
并产出块渲染图 `images/` + 块矢量资源 `sources/` + 块↔文本关系 `visualBlock_text_binding.json`；  
`blocks` 的 `captions.md` → related/author；全部 → `blocks_json` 组装最终 JSON。

> **重要变更（相对旧版）**：旧的「单张原子图提取 + `images_meta.json`」交付物已取消。  
> 原子对象仅作为可视逻辑块聚类的几何地基（落在 `过程文件/blocks/`），不再单独交付；  
> 矢量/Visio 源改由「块」持有（按 `block_id` 归档到 `sources/`）。

---

## 2. 核心产物 `<名>_visual_blocks.json`

### 2.1 顶层结构

```jsonc
{
  "$schema": "pptx_multimodal_slide_v2.0",
  "stem": "课件名",
  "tool_version": "pptx-wzq 1.5.0",      // 动态取自 __version__
  "generated_at": "2026-08-17T14:00:00",
  "slides": [ Slide, ... ],              // 全部页面（含无块页面）
  "summary": {
    "slides": 23,                        // 总页数
    "blocks_total": 43,                  // 总块数
    "slides_with_blocks": 23,            // 含块页数
    "block_types": {                     // 块类型分布
      "raster_image": 30, "native_table": 12, "native_logic_diagram": 1
    },
    "semantics_enriched": 43,            // DeepSeek 增强的块数
    "relations_enriched": 43             // DeepSeek 增强的跨模态关系数
  }
}
```

### 2.2 Slide 对象

```jsonc
{
  "slide_info": {
    "slide_id": "slide_001",
    "slide_index": 1,
    "layout_name": "",                   // 空（未解析，VLM/后续填充）
    "title": "第一章 战略管理导论",        // 启发式：页面首行非空文本前 60 字
    "notes_text": ""                     // 空（未解析）
  },
  "textual_content": {
    "raw_text": "页面全部文本…\n[公式] E=mc^2",   // 含 [公式] 标记追加页公式
    "semantic_summary": "",              // 空（VLM 模式填充）
    "reading_order": ["title_shape_1", "text_body_1", "blk_01", "..."]
  },
  "visual_blocks": [ VisualBlock, ... ],
  "cross_modal_relations": [ CrossModalRelation, ... ]
}
```

### 2.3 VisualBlock 对象

```jsonc
{
  "block_id": "blk_01",
  "block_type": "raster_image",          // 见 §2.6 类型全集
  "bbox": { "x": 954.0, "y": 610.0, "w": 275.0, "h": 74.0 },  // 幻灯片坐标 px
  "z_index_range": [1000, 1001],         // 成员层叠范围
  "center": { "x": 1091.5, "y": 647.0 }, // 块中心
  "text_density": 0.0,                   // 文字空间密度 = 块内文本字符数/块面积×10⁶
  "page": 1,
  "vector_resources": [                  // 块内矢量/Visio 成员（落盘文件名，供 sources/ 复制）
    "slide_05_visio_02.vsdx"             // 无则 []（纯像素图/逻辑图块为空）
  ],
  "assets": {
    "rendered_image": "./images/slide_01_blk_01.png",  // 块渲染图（bbox 无效时为 null）
    "vector_svg": "./sources/slide_01_blk_01.svg",     // 块矢量资源路径（sources/，无则 null）
    "internal_resources": []             // 块内嵌入资源
  },
  "internal_structure": {
    "topology_type": "isolated_nodes",   // grid_matrix | tree | directed_graph | isolated_nodes
    "nodes": [{
      "id": "s01_r001",                  // 原子对象 obj_id
      "type": "raster",                  // shape|connector|raster|vector|table|chart...
      "text": "",                        // 节点文本（短标签）
      "bbox_norm": [0.5, 2.0, 1.0, 3.0], // 归一化坐标 [x/w, y/h, (x+w)/w, (y+h)/h]
      "label": "picture 2"               // 对象名
    }],
    "edges": [{                          // 图/树边（connector 优先 + 空间最近邻）
      "source": "s01_sp002",
      "target": "s01_sp007",
      "label": "发送 SYN",
      "line_style": "solid_arrow"
    }],
    "table_matrix": null,                // 表格行数据（native_table 时 [[...], ...]）
    "formula_list": []                   // 公式引用（方案A后恒空）
  },
  "semantic_description": {              // DeepSeek 增强 / VLM / 规则回退 三级
    "block_type": "raster_image",
    "expression_goal": "展示战略管理导论的核心概念",       // ≤40 字
    "expression_role": "将抽象概念具象化，帮助学生直观理解",  // ≤70 字
    "expression_features": ["示意图", "分类结构"],        // 2~5 个抽象特征
    "vlm_caption": "该块为一张光栅图像…（100~200 字教学描述）",
    "teaching_use": "适用于工商管理类课程导论教学"         // 30~60 字
  }
}
```

### 2.4 CrossModalRelation 对象（DeepSeek 增强后）

```jsonc
{
  "relation_id": "rel_01_01",
  "text_anchor": "第一章 战略管理导论",          // 正文中与块关联最强的原句（≤60 字）
  "target_block_id": "blk_01",
  "relation_type": "title_caption",              // elaboration|title_caption|contrast|example|data_presentation
  "semantic_link": "页面标题直接定义了该块的表达主题"  // 图文逻辑关系陈述（≤120 字）
}
```

### 2.5 生成链（三级语义）

| 级别          | 触发                                                     | 内容                                                                              |
| ----------- | ------------------------------------------------------ | ------------------------------------------------------------------------------- |
| DeepSeek 增强 | `--semantic-model deepseek-v4-flash`（blocks_json 步骤默认） | 逐块生成 semantic_description + 逐条生成 cross_modal_relations（原料=块结构+该页文本/公式），失败回退保留原值 |
| VLM         | qwen3.7-plus（DASHSCOPE，blocks 步骤可选）                    | 视觉真值 caption                                                                    |
| 规则回退        | 无 client                                               | 模板填充                                                                            |

### 2.6 块类型全集

| 类型                                  | 含义                        |
| ----------------------------------- | ------------------------- |
| `raster_image` / `vector_image`     | 独立像素图 / 独立矢量图             |
| `visio_diagram`                     | 独立 Visio 对象（vsdx/vsd）     |
| `native_table`                      | 原生 PPT 表格（含 table_matrix） |
| `virtual_table`                     | 虚拟表格（VLM 候选；规则层不输出）       |
| `native_logic_diagram`              | 纯 shape/文本框构成的逻辑图         |
| `decorated_diagram`                 | 图片+形状/箭头复合的装饰逻辑图          |
| `chart`                             | 图表对象                      |
| `formula_block`                     | 公式（已剔除，不出现）               |
| `title_region` / `body_text_region` | 文本区（已剔除，不出现）              |

---

## 3. 文本类文件

### 3.1 `<名>_texts.md`（步骤1 text）

```markdown
# <课件名> 文本清单

> 由 `pptx-text` 提取（共 23 页）。每个文本对象一行；ID 页内递增；已排除页眉/页脚/页码/母版固定文本
> 格式：`ID | 类型 | 文本 | 坐标`；`[标题]` 来自标题占位符，其余为 `[内容]`（含表格行）。坐标=(x,y) 宽x高（px）。

## 第 1 页

| ID | 类型 | 文本 | 坐标 |
|---|---|---|---|
| TXT001-01 | 内容 | 第一章  战略管理导论 | (400,288) 463x53 |
```

- ID：`TXT{页:03d}-{页内序号:02d}`；类型：标题/内容/表格行。

### 3.2 `<名>_text_entries.json`（步骤1 审计数据）

```jsonc
[{
  "page": 1,
  "id": "TXT001-01",
  "type": "内容",
  "text": "第一章 战略管理导论",
  "shape": "textbox 4",       // 形状名
  "ph": "",                   // 占位符类型（title/body/obj/...）
  "x": 400.46, "y": 288.94,   // 幻灯片坐标 px
  "w": 463.2, "h": 53.8       // 宽高 px
}]
```

### 3.3 `<名>_formulas.md`（步骤2 formula）

```markdown
# <课件名> 公式清单

> 由 `extract_pptx_images.py` 自动提取（v4 三路径 + v5 符号判据过滤）...
> 仅 `progId` 含 equation 的 OLE 进入公式清单...

公式总数 **0** 条
- 原生 OMML（路径1）：0
- EQ3-MTEF 解析（路径2）：0
- OCR 识别（路径3）：0
```

- 有公式时每页 `## 第 N 页` + `- $LaTeX$` 条目。

### 3.4 `<名>_captions.md`（步骤3 blocks 产出，块级）

```markdown
# images 图片 AI 解读（可视逻辑块级）

> 由 `pptx-blocks` 生成 · 模型 `qwen3.7-plus` · 每块一条；块渲染图见 images/ 目录。

## 第 3 页

### IMG0001 — `slide_03_blk_01.png`  ✅

<该块的解读文本>
```

- 每页一节，每块一条；IMG 编号全局递增；`✅` 为成功标记。

### 3.5 `<名>_textbook.md`（步骤5 author）

```markdown
# <名>_textbook 教材文案

> 由 `pptx-author` 生成 · 模型 `deepseek-v4-flash` · 学科：工商管理
> 依据：文本/公式/图片解读三份 PPT 提取文档，共 23 页，其中 4 页原文直出。

## 第 1 页

第一章 战略管理导论
战略管理是工商管理专业中……（≥300 字教材口吻文案；原文超阈值页直接提取）

---
共 23 页（N 批），学科：工商管理。
```

- 直出页标注 `> 原文直出（超过 N 字，未扩写）。` 位于节标题后、内容前。

---

## 4. 可视化块交付物（块级）

### 4.1 `<名>_visualBlock_text_binding.json`（块 ↔ 文本 图文关联）

```jsonc
{
  "$schema": "pptx_visual_block_text_binding_v1.0",
  "stem": "课件名",
  "tool_version": "pptx-wzq 1.5.0",
  "generated_at": "2026-08-17T14:00:00",
  "summary": {
    "slides": 23,
    "blocks_total": 43,
    "bindings_total": 43,        // 关联条数（= 可视逻辑块数，每块至少一条）
    "sources_total": 2           // 复制到 sources/ 的块矢量资源数
  },
  "bindings": [
    {
      "relation_id": "rel_01_01",
      "page": 1,
      "target_block_id": "blk_01",
      "block_type": "raster_image",
      "text_anchor": "第一章 战略管理导论",   // 正文中与块关联最强的原句（≤60 字）
      "relation_type": "title_caption",       // elaboration|title_caption|contrast|example|data_presentation
      "semantic_link": "页面标题直接定义了该块的表达主题"  // 图文逻辑陈述（≤120 字）
    },
    ...
  ]
}
```

- 该文件是 `visual_blocks.json` 中 `cross_modal_relations` 的**独立视图**，专供「图文关联」检索/展示使用。
- 每条 binding 与 `visual_blocks.json` 的 `cross_modal_relations[].relation_id` 一一对应。

### 4.2 `images/`（块渲染图）

- 仅含可视逻辑块渲染图；命名 `slide_{页:02d}_{块id}.png`（与 `captions.md` 的 IMG 编号、`visual_blocks.json` 的 `assets.rendered_image` 对应）。
- **不再包含**单张原子图片（旧的「单图提取」已并入可视逻辑块）。

### 4.3 `sources/`（块矢量资源）

- 仅含「可视逻辑块所持有的矢量/Visio 源文件」：命名 `slide_{页:02d}_{块id}.{ext}`（ext ∈ vsdx/vsd/svg/wmf/emf）。
- 来源：块成员中 `kind ∈ {vector, visio, chart}` 且 `original_format` 为矢量格式的对象，其 `output_file` 从 `过程文件/blocks/by_page/` 复制而来。
- 某课件若无矢量/Visio 块，则 `sources/` 目录不生成。

---

## 5. 结构化中间文件

### 5.1 `atomic_objects.json`（步骤3 blocks 自举准备，可视逻辑块几何地基）

> 原 `img` 步骤已并入 `blocks`：在 `blocks` 步骤内由 `extract_pptx_images.prepare_block_inputs()` 生成，落于 `过程文件/blocks/atomic_objects.json`（并同时产出 `过程文件/blocks/by_page/` 媒体/矢量源工作副本）。

```jsonc
[{
  "obj_id": "s01_sp000",            // s{页:02d}_{类型缩写}{序号:03d}
  "page": 1,
  "kind": "shape",                  // shape|connector|raster|vector|visio|table|chart|formula|text_region
  "shape_name": "textbox 4",
  "text": "第一章 战略管理导论",
  "bbox": {"x": 400.46, "y": 288.94, "w": 463.2, "h": 53.8},
  "z_index": 0,
  "source_media": "",               // PPT 包内媒体路径
  "output_file": "",                // 落盘媒体文件名（矢量源有值，供 sources/ 复制）
  "original_format": "",
  "children": [],                   // 表格行数据 [[cell,...],...]
  "ph_type": ""                     // 占位符类型（text_region 判定依据）
}]
```

### 5.2 `manifest.json` / `manifest.csv`（blocks 自举准备的中间产物）

```jsonc
[{
  "page": 1, "index": 1,
  "kind": "picture",                // picture|fill|background|formula_ole|formula_omath|chart|visio
  "shape_name": "picture 2",
  "source_media": "image1.jpeg",
  "output_file": "slide_01_pic_01.png",
  "width": 4000, "height": 122,     // 图片物理像素
  "original_format": "jpeg",
  "converted_to_png": true,
  "note": "",
  "src_rect": null, "cropped": "",
  "x": 0.0, "y": 533.84,            // 幻灯片坐标 px
  "shape_w": 1280.0, "shape_h": 38.67,  // 显示尺寸 px
  "ole_progid": "", "preview_file": ""
}]
```

### 5.3 `<名>_related_filter.json`（步骤4 related 审计）

```jsonc
{
  "stem": "课件名",
  "model": "deepseek-v4-flash",
  "total": 43, "kept": 40, "dropped": 3,
  "reasons": { "logo": 2, "页眉装饰": 1 },        // 删除原因计数
  "dropped_items": {
    "slide_05_blk_02.png": {"page": 5, "reason": "logo", "caption": "（截断）"}
  }
}
```

---

## 6. 运行状态文件

### 6.1 `state.json`（断点续传状态机）

```jsonc
{
  "pptx": "C:/.../课件.pptx",
  "stem": "课件名",
  "doc_md5": "a1b2c3...",            // 源文档 md5（换源检测）
  "tool_version": "pptx-wzq 1.5.0",
  "started_at": "2026-08-17T14:00:00",
  "steps": {
    "text":       {"status": "done",      "note": "", "updated_at": "..."},
    "formula":    {"status": "done",      "note": "", "updated_at": "..."},
    "blocks":     {"status": "done",      "note": "", "updated_at": "..."},
    "related":    {"status": "done",      "note": "", "updated_at": "..."},
    "author":     {"status": "done",      "note": "", "updated_at": "..."},
    "blocks_json": {"status": "done",     "note": "", "updated_at": "..."}
  }
}
```

- status：`pending / running / partial / done / failed`；原子写（临时文件+rename）。

### 6.2 `pipeline.log`

```
[2026-08-17 14:00:00] 步骤 text 开始（run）
[2026-08-17 14:05:12] 步骤 text 完成
[2026-08-17 14:05:12] 步骤 formula 开始（run）
...
[2026-08-17 14:40:00] 全部步骤完成，产物已归位
```

---

## 7. 目录命名与资源约定

| 目录/文件      | 命名规则                                                                         | 说明           |
| ---------- | ---------------------------------------------------------------------------- | ------------ |
| `images/`  | `slide_{页:02d}_{块id}.png`                                                  | 仅可视逻辑块渲染图 |
| `sources/` | `slide_{页:02d}_{块id}.{ext}`（ext∈vsdx/vsd/svg/wmf/emf）                       | 块持有的矢量/Visio 资源 |
| 过程文件/      | 与步骤名对应（blocks/text/formula）                                              | 溯源/二次加工      |

---

## 8. 附：结果文件与步骤对应表

| 结果文件                           | 产出步骤               | 消费方                       |
| ------------------------------ | ------------------ | ------------------------- |
| `visual_blocks.json`           | 3（工作副本）→ 6（最终）     | RAG 检索/知识库/排版             |
| `visualBlock_text_binding.json`| 6（blocks_json 归位）     | 图文关联检索/展示                 |
| `captions.md`                  | 3 → 4（过滤重写）→ 6（归位） | author 输入/配图说明            |
| `textbook.md`                  | 5                  | 教材/课件文字素材                 |
| `texts.md`/`text_entries.json` | 1                  | blocks/related/author 上下文 |
| `formulas.md`                  | 2                  | blocks/author 上下文         |
| `atomic_objects.json`/`by_page/`| 3（blocks 自举准备）      | blocks 聚类几何地基              |
| `sources/`                     | 6（blocks_json 归位）     | 块矢量资源交付                   |
| `related_filter.json`          | 4                  | 过滤审计                      |
| `state.json`/`pipeline.log`    | 全程                 | 断点续传/日志                   |
