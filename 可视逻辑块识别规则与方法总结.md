# 可视逻辑块识别规则与方法 · 技术总结

> 版本：pptx-wzq 1.5.x（visual_blocks.py / cli_blocks.py / extract_pptx_images.py）
> 日期：2026-08-16 · 作者：吴振谦
> 适用范围：PPT 课件 → 可视逻辑块（Visual Block）全栈解析 → 多模态教学 RAG 知识库

---

## 1. 概念定义

**可视逻辑块（Visual Block）**：PPT 一个页面中由多个各类对象（像素图、矢量图、shape、公式、文本框、箭头）组成的，占据一定面积的，明确表达一个主题逻辑，具有清晰表达目的和作用、服务于页面表达要求的**可视化对象集合**。

- 一个页面可能有 1~6 个可视逻辑块（`max_blocks_per_slide = 6`）；
- 页面**文本区（标题/正文占位符）不属于可视逻辑块**——它们已在 `textual_content` 中，不参与聚类；
- **公式不属于可视逻辑块**——公式是文本流内容，已由公式提取步骤（`formulas.md`）完整保留。

---

## 2. 识别流程总览（9 步判据链）

```
PPTX 内部对象树遍历
   ↓
① 原子对象提取（图片/形状/连接符/表格 → bbox + 类型 + z 序）
   ↓
② text_region 剔除（标题/正文/页眉页脚占位符，页面文本区）
   ↓
③ formula 剔除（OLE/OMML 公式一律退出，方案A）
   ↓
④ 噪声过滤 _filter_noise
   ├─ 越界对象（中心在页面外）剔除
   ├─ 装饰小 shape（<48px / 细长 >10:1）剔除
   ├─ 小型公式/符号碎片（面积 <20000px²）剔除
   └─ 整页宽横幅（≥90% 页宽且 ≤80px 高，页眉色带）剔除
   ↓
⑤ 大图独立（raster/vector/visio 面积 > 30% 页面 → 强制独立成块）
   ↓
⑥ 四向种子扩展区域生长 _region_grow（加法生长）
   ├─ 种子对象 → 上下左右扩展紧邻对象
   ├─ >10 字文本对象 = 「墙」：不作种子、不被并入，生长碰到即停
   └─ 间隙规则：双方有文本（短标签）放宽至 100px；纯图形 40px
   ↓
⑦ 文本密度双校验
   ├─ 预筛：单 shape 文本 >10 字 → 正文文本框，不参与聚类
   └─ 块级：块内文本总量 >30 字 → 剔除长文本成员后重组
   ↓
⑧ 符号碎片过滤（块级）
   ├─ 无文本/无媒体/无 connector 的纯形状块，对象数 ≤3 → 剔除
   └─ 纯形状块对象数 >3 但面积 < 页面 10% → 剔除
   ↓
⑨ 块级后处理
   ├─ 限 6 块/页（按面积降序取前 6）
   ├─ 块类型判定 _guess_block_type
   ├─ 拓扑推断 _infer_topology（节点/边/表格矩阵）
   ├─ 语义描述（VLM 或规则回退）
   └─ 渲染图裁剪（真实页面尺寸换算）+ images/ 与 JSON 双向对齐
```

---

## 3. 原子对象提取（`extract_pptx_images.py`）

从 PPTX 包内逐页遍历 `<p:sp>`（原生形状）、`<p:cxnSp>`（连接符/箭头）、`<p:pic>`（像素图）、`<p:graphicFrame>`（表格/图表）、`<p:oleObj>`（OLE：公式/Visio）、`<m:oMath>`（OMML 公式）等对象，统一映射为原子对象：

| 字段 | 含义 |
|---|---|
| `obj_id` | 稳定 ID（`s{页:02d}_{类型缩写}{序号:03d}`） |
| `page` | 页码 |
| `kind` | `shape` / `connector` / `raster` / `vector` / `visio` / `table` / `chart` / `formula` / `text_region` |
| `shape_name` | PPT 中对象名 |
| `text` | shape 内文本 |
| `bbox` | 幻灯片坐标（px，`{x, y, w, h}`，EMU→96dpi） |
| `z_index` | 层叠顺序 |
| `output_file` | 落盘媒体文件名（图片/公式提取产物） |
| `original_format` | 原始格式（png/jpg/vsdx/wmf…） |
| `children` | 表格行数据（`[[cell,...],...]`） |

提取产物：`atomic_objects.json`（全课件）、`manifest.json/csv`（图片清单）、`by_page/`（媒体文件）。

---

## 4. 逐条规则详解

### 4.1 页面文本区剔除（`kind = "text_region"`）

标题/正文/副标题/内容占位符（`<p:ph>` type 为 title/ctrTitle/subTitle/body/obj）的 bbox 通常覆盖页面大半且互相重叠——若不剔除，会把页内所有可视化对象链式串并成"整页一块"。

**规则**：`iter_native_shapes` 解析占位符类型，占位符 shape 标记 `kind="text_region"`，聚类前剔除。页面文本内容由 `pptx-text` 提取步骤保留。

### 4.2 公式剔除（方案 A）

公式原子对象（`kind="formula"`，来自 OLE/OMML）普遍带 `output_file`（提取落盘产物），若按"有媒体文件→主内容保留"处理会被无条件放行、参与聚类并入块。

**规则**：`_filter_noise` **入口**处 `if o.kind == "formula": continue`——无论大小、有无 output_file、有无文本，公式一律不参与可视逻辑块。

### 4.3 噪声过滤（`_filter_noise`，吸收 img_filter 教学判据）

按顺序判定，命中即保留/剔除：

| # | 判据 | 阈值 | 处理 |
|---|---|---|---|
| 1 | formula | — | 一律剔除（4.2） |
| 2 | 有文本（逻辑图节点/箭头标注，即使小） | — | 保留 |
| 3 | 整页宽横幅（页眉/页脚色带） | 宽 ≥90% 页宽 且 0<高 ≤80px | 剔除 |
| 4 | 有媒体文件（raster/vector/visio/chart 主内容） | — | 保留 |
| 5 | table（主内容） | — | 保留 |
| 6 | 小型公式符号（无媒体、无文本） | 面积 <20000px²（≈141×141） | 剔除 |
| 7 | 无文本 shape/connector 装饰 | 面积 <1600px² 或 宽或高 <48px 或 宽高比 >10 | 剔除 |
| 8 | 其余无文本 shape | — | 保留 |

另：**越界剔除**——中心落在页面外（x∈[0,page_w]、y∈[0,page_h]）的页外装饰对象，在聚类前剔除。**页面尺寸取真实 `sldSz`**（`read_sld_size`，16:9 页面按 1280×720 判定），不再固定用 4:3 参考值。

### 4.4 大图独立（用户准则①）

**规则**：raster/vector/visio 对象面积 > 整页 30%（`big_area_ratio=0.30`，960×720 页下 ≈207,360px²）→ **强制独立成块**，不与邻近小对象合并。整页一张大电路图/波形图即属此类。

### 4.5 四向种子扩展区域生长（用户算法准则，核心）

> 用户准则原文：深入 PPTX 内部对象树遍历每个对象，计算空间位置与占地面积，从种子对象出发沿**四个方向**扩展寻找其他对象，直到碰到**字多的文本对象（墙）**或**页面边界**为止。

**算法**（`_region_grow`，四向 BFS 加法生长）：

```
对每个未访问对象作为种子：
    标记访问，入栈
    while 栈非空:
        弹出 cur
        对所有未访问、非墙的对象 other:
            若 _is_adjacent(cur, other) → 并入本区域（访问标记 + 入栈）
```

- **文本墙**：`text` 去空白 > `max_shape_text(10)` 字的对象，既不作种子、也不被并入——生长碰到即停（页面上多字文本框自然分隔出多个区域）；
- **凹区域**：被墙/间隙天然阻断成多个区域，**无需凸分割**（减法算法被替换）；
- **表格/框图**：单元格/节点四向紧邻 → 自然长成一片，**不会被切碎**（加法天然保整）。

**邻接判定** `_is_adjacent`（方向区分）：

```
左右相邻：水平间隙 ≤ 阈值 且 垂直间隙 ≤ gap_max（有重叠/接近）
上下相邻：垂直间隙 ≤ 阈值 且 水平间隙 ≤ gap_max
阈值 = 双方都有文本(短标签) → text_gap(100px)   # 表格单元格/框图节点倾向同属一结构
       否则（纯图形）         → gap_max(40px)
```

### 4.6 文本密度双校验（用户准则）

> 用户准则原文：可视逻辑块内文本框应是短标签（节点名/箭头标注），>10 字属正文；块内文本 >30 字必混入文本区；文字空间密度可作为初始判据。

| 校验 | 规则 | 作用 |
|---|---|---|
| 预筛 | 单 shape 文本 >10 字 → 不参与聚类 | 正文文本框直接排除 |
| 块级 | 块内文本总量 >30 字 → 剔除长文本成员后重组（剩 1 个有效对象则保留为单对象块） | 兜底防漏网正文混入 |
| 审计 | 每块输出 `text_density` = 块内文本字符数 / 块面积 ×10⁶ | 高密度=文本区、低密度=逻辑图，判据可审计 |

### 4.7 符号碎片过滤（用户准则②，块级）

> 用户准则原文：面积少于页面 20% 的可视逻辑图可忽略；可视逻辑图不应有超小面积图块。

对**无文本、无媒体（图/表/visio）、无 connector 的纯形状块**：

| 判据 | 规则 |
|---|---|
| 碎片语义 | 对象数 ≤3 → 无条件剔除（单个/成对符号，如 54×57 的电路元件符号） |
| 块级最小面积 | 对象数 >3 但块面积 < 页面 `min_block_area_ratio(0.10)`（4:3 页 ≈263×263px；按真实页面面积计算）→ 剔除 |

**豁免**（不误伤）：含文本标签（逻辑图节点短标签）、含图/表/visio（独立小图 4.3% 页也保留）、含 connector（显式逻辑图）。

### 4.8 每页块数上限

按簇面积降序取前 `max_blocks_per_slide(6)` 个块。

---

## 5. 块类型判定（`_guess_block_type`）

| 条件 | 块类型 |
|---|---|
| 单对象 | 按 `SINGLE_KIND_TO_TYPE` 映射：raster→`raster_image`、vector→`vector_image`、visio→`visio_diagram`、chart→`chart`、table→`native_table`、formula→`formula_block`（公式已剔除，实际不会出现） |
| 含 table | `native_table` |
| 全 shape/connector/textbox（无媒体） | `native_logic_diagram`（原生逻辑图） |
| 有媒体（raster/vector/visio）+ 有 shape/connector | `decorated_diagram`（复合装饰图） |
| 仅有媒体 | `raster_image` |
| 其他 | `native_logic_diagram` |

> 注：VLM 模式下由 `describe_block` 判定覆盖规则判定。

---

## 6. 拓扑推断（`_infer_topology`）

| 内容 | 规则 |
|---|---|
| `nodes[]` | 每个非 connector、非 formula 成员 → `{id, type, text, bbox_norm(归一化坐标), label}` |
| `edges[]` | ① connector 显式连接（stCxn/endCxn → 节点）优先；② 无 connector 时空间最近邻启发式（右/下优先） |
| `table_matrix` | 含 table 成员时取 `children` 行数据 |
| `formula_list` | 块内公式引用（方案 A 后恒为空） |
| `topology_type` | `grid_matrix`（有表格）/ `tree`（有边且为树）/ `directed_graph`（有边）/ `isolated_nodes`（无边） |

---

## 7. 语义描述（`semantic_description`）

VLM 模式（qwen3.7-plus，去掉 `--no-vlm` 并配置 `DASHSCOPE_API_KEY`）：块渲染图 + 该页文本/公式上下文 → 生成 5 个字段。

规则回退模式（`--no-vlm`，当前默认）：模板填充（`expression_goal="展示本页的可视化内容"` 等）。

| 字段 | 含义 |
|---|---|
| `expression_goal` | 表达目标（该块要展示什么） |
| `expression_role` | 表达角色/方式（具象化、对比、时序…，支撑 RAG 语义检索） |
| `expression_features` | 抽象表达特征列表（如「可视化」「垂直时序」） |
| `vlm_caption` | 视觉模型对块的详细描述（替代原 image captioning） |
| `teaching_use` | 教学用途建议 |

---

## 8. 跨模态关系（`cross_modal_relations`）

块与页面文字的语义关联（DeepSeek 生成，规则回退）：

| 字段 | 含义 |
|---|---|
| `relation_id` | `rel_NN` |
| `text_anchor` | 关联的页面文字片段 |
| `target_block_id` | 目标块 ID |
| `relation_type` | `elaboration`（展开说明）/ `title_caption`（标题定义主题）等 |
| `semantic_link` | 文字与块的逻辑关系陈述 |

---

## 9. 渲染与资源组织

- **渲染管线**：LibreOffice 整页渲染（soffice → PDF → pdftoppm，150dpi）→ 按块 bbox 裁剪 PNG；整份课件只渲染一次 + 缓存复用（课件旁 `.render_cache/`）；
- **关键修复**：裁剪换算必须传入**真实页面尺寸** `sldSz`（16:9 页面若不传，按 4:3 默认值换算会把块左侧内容切掉）；
- **images/ 与 JSON 双向一致**：渲染完成后兜底清理未被 JSON 引用的渲染图（缺失 0、多余 0）；
- 资源组织：`images/`（全部块渲染图，`slide_{页:02d}_{块}.png`）、`sources/`（矢量源归档 vsdx/svg/wmf/emf）。

---

## 10. JSON 数据结构（`pptx_multimodal_slide_v2.0`）

### 10.1 顶层结构

```jsonc
{
  "$schema": "pptx_multimodal_slide_v2.0",
  "stem": "课件名",
  "tool_version": "pptx-wzq 1.5.0",
  "generated_at": "ISO 时间",
  "slides": [ Slide, ... ],          // 全部页面（含无块页面）
  "summary": {
    "slides": 231,                   // 总页数
    "blocks_total": 280,             // 总块数
    "slides_with_blocks": 186,       // 含块页数
    "block_types": {                 // 类型分布
      "raster_image": 144,
      "native_logic_diagram": 29,
      "decorated_diagram": 72,
      "vector_image": 33,
      "native_table": 2
    }
  }
}
```

### 10.2 Slide 对象

```jsonc
{
  "slide_info": {
    "slide_id": "slide_041",
    "slide_index": 41,
    "layout_name": "",            // 当前为空字符串（未解析布局名，VLM/后续填充）
    "title": "页面标题",           // 启发式：取页面首行非空文本前 60 字
    "notes_text": ""              // 当前为空字符串（未解析备注，VLM/后续填充）
  },
  "textual_content": {
    "raw_text": "页面全部文本（含 [公式] 标记）",
    "semantic_summary": "",       // 当前为空字符串（VLM 模式下填充）
    "reading_order": ["title_shape_41", "text_body_41", "blk_01", "..."]
  },
  "visual_blocks": [ VisualBlock, ... ],
  "cross_modal_relations": [ CrossModalRelation, ... ]
}
```

### 10.3 VisualBlock 对象（真实示例，高频页 41）

```jsonc
{
  "block_id": "blk_01",
  "block_type": "native_logic_diagram",     // 见 §5 类型表
  "bbox": { "x": 238.1, "y": 375.2, "w": 635.0, "h": 310.0 },  // 幻灯片坐标 px
  "z_index_range": [2, 7],                  // 成员层叠范围
  "center": { "x": 555.6, "y": 530.2 },     // 块中心
  "text_density": 0.0,                      // 文本空间密度（×10⁶，可审计）
  "page": 41,
  "assets": {
    "rendered_image": "./images/slide_41_blk_01.png",  // 块渲染图
    "vector_svg": null,                       // 矢量源（sources/）
    "internal_resources": []                  // 块内嵌入资源
  },
  "internal_structure": {
    "topology_type": "directed_graph",       // grid_matrix | tree | directed_graph | isolated_nodes
    "nodes": [{
      "id": "s41_sp002",
      "type": "shape",                       // shape | connector | raster | vector | table ...
      "text": "",                            // 节点文本（短标签）
      "bbox_norm": [0.788, 2.789, 1.788, 3.789],  // 归一化坐标 [x/w, y/h, (x+w)/w, (y+h)/h]
      "label": "Shape2"                      // 对象名
    }],
    "edges": [{
      "source": "s41_sp002",
      "target": "s41_sp007",
      "label": "",                           // 边标注（如"发送 SYN"）
      "line_style": "solid_arrow"
    }],
    "table_matrix": null,                    // 表格行数据 [[...], ...]（native_table 时）
    "formula_list": []                       // 公式引用（方案 A 后恒空）
  },
  "semantic_description": {                  // §7：VLM 生成或规则回退
    "block_type": "native_logic_diagram",
    "expression_goal": "展示本页的可视化内容",
    "expression_role": "通过可视化方式辅助理解本页知识点（规则回退）",
    "expression_features": ["可视化"],
    "vlm_caption": "本块包含 6 个对象。",
    "teaching_use": "教学辅助图示"
  }
}
```

### 10.4 支持的块类型全集

| 类型 | 含义 |
|---|---|
| `raster_image` | 独立像素图 |
| `vector_image` | 独立矢量图 |
| `visio_diagram` | 独立 Visio 对象（vsdx/vsd） |
| `native_table` | 原生 PPT 表格（含 table_matrix） |
| `virtual_table` | 虚拟表格（形状网格，VLM 候选类型；凸分割时代产生，区域生长后规则层不再输出） |
| `native_logic_diagram` | 纯 shape/文本框构成的逻辑图（无图片媒体） |
| `decorated_diagram` | 图片 + 形状/箭头复合的装饰逻辑图（VLM caption 主战场） |
| `chart` | 图表对象 |
| `formula_block` | 公式（VLM 候选类型；公式已剔除，实际不出现） |
| `title_region` / `body_text_region` | 标题区/正文区（VLM 候选类型；页面文本区已剔除，规则层不输出） |

---

## 11. 参数表（`CLUSTER_CONFIG`，visual_blocks.py）

| 参数 | 默认值 | 含义 |
|---|---|---|
| `min_area` | 1600 | 无文本 shape 最小面积（px²） |
| `alpha_h` / `alpha_v` | 1.0 | （保留字段，区域生长已改用恒定间隙） |
| `max_gap_px` | 40 | 纯图形邻接最大间隙（px） |
| `text_gap_px` | 100 | 双方有文本（短标签）时的放宽间隙（px） |
| `max_blocks_per_slide` | 6 | 每页最多块数 |
| `page_w` / `page_h` | 960 / 720 | 页面参考尺寸（px，4:3 默认值）。`extract_blocks` 运行时用真实 `sldSz` 覆盖（16:9 页面自动变 1280×720），越界/横幅/大图/碎片比例均按真实页面计算 |
| `max_shape_text` | 10 | 单 shape 文本 >10 字 → 正文墙/预筛 |
| `max_block_text` | 30 | 块内文本 >30 字 → 剔除长文本成员 |
| `min_size` | 48 | 无文本 shape 宽或高下限（px） |
| `max_ratio` | 10 | 无文本 shape 宽高比上限（细长剔除） |
| `formula_min_area` | 20000 | 小型公式符号面积下限（px²） |
| `big_area_ratio` | 0.30 | 大图独立：面积 >30% 页面 |
| `split_gap_px` | 40 | （保留字段，凸分割已由区域生长替代） |
| `min_sub_area_ratio` | 0.20 | （保留字段，凸分割保护） |
| `banner_ratio` | 0.9 | 横幅：宽 ≥90% 页面 |
| `banner_max_h` | 80 | 横幅：高 ≤80px |
| `min_block_area_ratio` | 0.10 | 碎片块最小面积：纯形状块 <10% 页面剔除 |

---

## 12. 局限与后续方向

1. **纯几何规则的上限**：分割目前完全由几何规则驱动（墙 + 间隙 + 面积），"距离近但语义无关"的对象仍可能误并——方案预留了 **VLM 对候选簇二次校验**（合并/拆分决策）未落地；
2. **connector 分支未在真实数据充分验证**：高频/战略管理课件均无 `<p:cxnSp>` 箭头对象，拓扑的 connector 优先路径只过了单测；
3. **语义描述为规则回退**：当前 `--no-vlm` 模式下 `semantic_description` 是模板填充，接入 qwen3.7-plus 后可获得真实表达目的/角色，支撑"基于意图的组件检索"等 RAG 应用；
4. **文本墙阈值可配置**：`max_shape_text=10` 对中文字符与英文 token 的语义密度差异未区分，可后续调优。

---

## 13. 硬编码清单与改进建议（2026-08-16 核对并修复 A~D）

对 `visual_blocks.py` / `cli_blocks.py` / `extract_pptx_images.py` 逐行核对，硬编码点如下（**A~D 已修复**，E/F 保留为建议）：

| # | 位置 | 硬编码 | 状态 | 修复内容 |
|---|---|---|---|---|
| **A** | `cli_blocks.py` 顶层 binding | `"tool_version": "pptx-wzq 1.5.0"` | ✅ 已修复 | 改为 `"pptx-wzq " + __import__("pptx_wzq").__version__` 动态取 |
| **B** | `CLUSTER_CONFIG.page_w/page_h = 960×720` | 比例类判据固定用 4:3 参考值 | ✅ 已修复 | `extract_blocks` 用 `read_sld_size` 读取真实 `sldSz` 注入 cfg，越界/横幅/大图/碎片比例按真实页面算（战略管理 6 个页面右侧对象回归聚类，实测 6/6） |
| **C** | `_is_adjacent` 默认参数 | `text_gap = gap_max * 2.5` | ✅ 已修复 | 默认回退改为 `gap_max`，统一口径（调用方显式传 cfg 的 `text_gap_px`） |
| **D** | 渲染 dpi | `dpi=150` 固定 | ✅ 已修复 | 新增 `--render-dpi` 参数（默认 150），`render_pptx_pages`/`_crop_block_png` 透传 |
| E | VLM 提示词截断 | `page_text[:1200]`、`[:1500]`、`vlm_caption[:200]` 等 | 保留 | Token 控制截断，常规做法；如需可配置化（低优先级） |
| F | `_page_title` | 取页面首行非空文本前 60 字 | 保留 | 可改用 `text_entries.json` 的标题条目（type=标题） |

**合理硬编码（不算问题）**：EMU↔px 换算常数 `914400`（1 英寸=914400 EMU，标准值）、96dpi 逻辑像素假设、`max_gap_px=40`/`text_gap_px=100` 等聚类阈值（已在 CLUSTER_CONFIG 中集中管理，可整体调整）。

**文档核对结论**：本文档 §1~§12 与当前代码逐条核对一致（§5 单对象映射、§10.2 slide_info 空字段、§10.4 类型全集、§11 page_w 注释为本轮修正项）；§12 局限中"connector 未真实验证"等仍成立。

---

*附：实现文件 — `src/pptx_wzq/visual_blocks.py`（核心库）、`src/pptx_wzq/cli_blocks.py`（CLI 入口 pptx-blocks）、`src/pptx_wzq/extract_pptx_images.py`（原子对象提取）。*
