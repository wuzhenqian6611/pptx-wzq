# pptx-kb 可视逻辑块（Visual Block）提取技术方案与工作提示词

> 版本：v1.0 · 日期：2026-08-16
> 适用：`pptx-kb`（`pptx-wzq` ≥1.4.0）
> 目标：把当前「单图/单公式/单图表」提取升级为「可视逻辑块（Visual Block）」全栈解析，替换现有 `*_binding.json`，同时保留原有文本/公式/扩写流程与输出目录结构。

---

## 1. 需求理解：从 8 种对象到 1 种抽象

用户识别出当前代码能/不能处理的 8 类对象：

| # | 当前能力 | 当前问题 | 可视逻辑块视角下的归类 |
|---|---------|---------|----------------------|
| 1 | 像素图 + srcRect | ✅ 可提取 | 单个 `raster_image` 块，或作为复合块的内部资源 |
| 2 | 部分 wmf/svg | ⚠️ 部分可提取 | `vector_image` 块或复合块的内部资源 |
| 3 | vsdx | ✅ 可提取 | `visio_diagram` 块 |
| 4 | 像素图/OLE/vsdx + PowerPoint 叠加文本/箭头/形状 | ❌ 解读差 | 需整体识别为一个 `native_logic_diagram` 或 `decorated_diagram` 块 |
| 5 | 纯 PowerPoint 绘制文本框/箭头/形状混合 | ❌ 不明确 | `native_logic_diagram` / `flowchart` / `concept_map` 块 |
| 6 | PowerPoint 表格对象 | ❌ 不明确 | `native_table` 块 |
| 7 | shape/文本框拼成的表格图像 | ❌ 不明确 | `virtual_table` 块 |
| 8 | 像素图 + 箭头/shape/文本框拼成的复杂流程图/逻辑层次图 | ❌ 不好 | `native_logic_diagram` / `flowchart` 块 |

**核心抽象**：

> 可视逻辑块（Visual Block）= 一个 PPT 页面上由若干对象（像素图、矢量图、shape、文本框、箭头、公式、图表）组成的、占据一定面积、表达一个主题逻辑、具有清晰教学目的的可视化对象集合。

输出目标：把每页拆成 1~N 个 Visual Block，并给出每个块的几何、拓扑、资源、内容、类型、抽象表达特征、教学语义、跨模态关系。

---

## 2. 输出 JSON Schema（在用户提供示例基础上扩充）

```json
{
  "$schema": "pptx_multimodal_slide_v2.0",
  "stem": "课件文件名（无扩展名）",
  "tool_version": "pptx-wzq 1.5.0",
  "generated_at": "2026-08-16T16:30:00",
  "slides": [
    {
      "slide_info": {
        "slide_id": "slide_012",
        "slide_index": 12,
        "layout_name": "Title and Content",
        "title": "TCP 三次握手与状态变迁",
        "notes_text": "教师备注（若有）"
      },
      "textual_content": {
        "raw_text": "本页完整拼接文本（含标题、正文、表格单元格文本，不含无意义占位符）",
        "semantic_summary": "一句话总结本页教学语义",
        "reading_order": ["title_shape_1", "text_body_2", "blk_01", "text_body_3"]
      },
      "visual_blocks": [
        {
          "block_id": "blk_01",
          "block_type": "native_logic_diagram",
          "bbox": {"x": 150, "y": 200, "width": 600, "height": 400, "rotation": 0},
          "center": {"x": 450, "y": 400},
          "z_index_range": [3, 8],
          "assets": {
            "rendered_image": "./images/slide_12_blk_01.png",
            "vector_svg": "./sources/slide_12_blk_01.svg",
            "internal_resources": [
              {"type": "embedded_image", "path": "./images/slide_12_blk_01_res_01.png", "desc": "服务器图标素材"}
            ]
          },
          "internal_structure": {
            "topology_type": "directed_graph",
            "nodes": [
              {"id": "node_1", "type": "shape", "text": "CLOSED", "bbox_norm": [0.1, 0.1, 0.2, 0.15], "label": "状态节点"}
            ],
            "edges": [
              {"source": "node_1", "target": "node_2", "label": "发送 SYN", "line_style": "solid_arrow"}
            ],
            "table_matrix": null,
            "formula_list": []
          },
          "semantic_description": {
            "expression_goal": "展示 TCP 协议中客户端与服务端建立连接的三次握手时序及状态机变迁。",
            "expression_role": "通过可视化时序图，将抽象的网络协议状态具象化，帮助学生理解连接建立的同步过程。",
            "expression_features": ["时序图", "状态机", "双向交互", "垂直布局"],
            "vlm_caption": "这是一张垂直时序图，左侧为客户端，右侧为服务端。展示了三次握手的报文交互...",
            "teaching_use": "适用于计算机网络课程，讲解 TCP 连接建立过程，可替换为 TLS/QUIC 握手等同类主题。"
          }
        }
      ],
      "cross_modal_relations": [
        {
          "relation_id": "rel_01",
          "text_anchor": "如图所示，建立连接需要三个步骤",
          "text_anchor_bbox": {"x": 100, "y": 150, "width": 300, "height": 20},
          "target_block_id": "blk_01",
          "relation_type": "elaboration",
          "semantic_link": "文本提出结论，blk_01 时序图详细展开并证明三个步骤的报文交互过程。"
        }
      ]
    }
  ],
  "summary": {
    "slides": 23,
    "blocks_total": 31,
    "slides_with_blocks": 18,
    "block_types": {"native_logic_diagram": 12, "raster_image": 8, "native_table": 3, "virtual_table": 2, "visio_diagram": 2, "vector_image": 2, "chart": 2}
  }
}
```

### 2.1 block_type 枚举（扩展版）

| block_type | 定义 | 典型内部对象 |
|-----------|------|-------------|
| `raster_image` | 独立像素图（照片、截图、位图素材） | `<p:pic>` 中的 png/jpg |
| `vector_image` | 独立矢量图（emf/wmf/svg） | `<p:pic>` 中的矢量 |
| `visio_diagram` | Visio OLE 嵌入图 | `oleObj` + `Visio.Drawing.*` |
| `chart` | Office 图表（柱/折/饼等） | `<c:chart>` |
| `native_table` | PPT 原生表格对象 | `<a:tbl>` |
| `virtual_table` | 用 shape/文本框拼成的伪表格 | 多个 `<p:sp>` 规则网格排列 |
| `native_logic_diagram` | PPT 原生 shape/箭头/文本框组成的逻辑图 | 多个 `<p:sp>` + 连接符 |
| `decorated_diagram` | 像素图/矢量图 + PPT 原生标注/箭头/文本框 | `<p:pic>` + `<p:sp>` |
| `formula_block` | 独立或一组公式 | `<m:oMath>` / ole 公式 |
| `title_region` | 页面标题区（通常不进入 visual_blocks，单列 reading_order） | 标题占位符 |
| `body_text_region` | 页面正文区（通常不进入 visual_blocks） | 正文占位符 |

---

## 3. 技术路线

### 3.1 核心思路

**两阶段策略**：

1. **自底向上聚合**：先按原流程提取所有原子对象（像素图、矢量图、Visio、公式、原生 shape、表格、文本框），保留每个对象的 bbox、文本、类型、层级。
2. **自顶向下聚类**：用空间邻近规则 + VLM 校验，把原子对象聚成 1~6 个 Visual Block；对单个独立对象直接形成一个块。

### 3.2 新增模块设计

新增 `src/pptx_wzq/visual_blocks.py`（约 600~900 行），对外暴露：

```python
def extract_blocks(pptx_path: str, out_dir: str,
                   atomic_objects: list[AtomicObject],
                   page_texts: dict[int, list],
                   client=None, model=None,
                   on_progress=None) -> list[SlideBlocks]:
    """输入原子对象 + 每页文本，输出每页可视逻辑块。"""

def render_block_to_png(slide_page: int, block: VisualBlock,
                        pptx_path: str, out_png: Path, dpi=150) -> bool:
    """用 LibreOffice / python-pptx 渲染单块为 PNG（用于 VLM 与 RAG）。"""

def describe_block(client, model, block: VisualBlock,
                   page_text: str) -> dict:
    """调用 VLM 生成 expression_goal/role/features/vlm_caption/teaching_use。"""

def build_cross_modal_relations(blocks: list[VisualBlock],
                                page_texts: list,
                                textbook_page: str) -> list[dict]:
    """生成文本锚点 ↔ block 的跨模态关系。"""
```

### 3.3 原子对象定义（由 extract_pptx_images 扩展而来）

在 `extract_pptx_images.py` 的 `extract()` 主流程中，新增对原生 shape、连接符、文本框、表格的解析，统一输出 `AtomicObject`（用 dataclass 或 dict），字段示例：

```python
@dataclass
class AtomicObject:
    page: int
    obj_id: str            # 全局唯一，如 "s12_o5"
    kind: str              # raster/vector/visio/chart/table/shape/connector/textbox/formula
    shape_name: str        # PowerPoint 中 shape 的名字
    text: str              # shape 内文本（若有）
    bbox: dict             # {x, y, w, h} px
    z_index: int           # 绘制顺序（用于判断覆盖关系）
    source_media: str      # 包内媒体路径（raster/vector/visio 有）
    output_file: str       # by_page/ 下保存的文件名
    original_format: str   # png/emf/wmf/svg/vsdx/...
    children: list         # 表格单元格 / 组合形状子对象
```

### 3.4 空间聚类算法

**Step 1：过滤无关原子对象**

- 删除面积过小（< 1600 px²）、全页背景、页眉页脚占位符、logo/装饰性小图标（可复用 `img_filter` 规则）。

**Step 2：构建候选邻接图**

- 对每个原子对象计算中心点 `(cx, cy)`。
- 若两个对象 bbox 水平/垂直距离 < `alpha * max(h1, h2)`（默认 alpha=1.5），则认为可能属于同一块。
- 若两个对象有重叠（IoU > 0 或一个包含另一个），强制连边。

**Step 3：连通分量聚类（DBSCAN 或并查集）**

- 用并查集把 Step 2 的邻接边合并成候选簇。
- 每个簇若只含 1 个对象且为独立 raster/vector/visio/chart，则直接判定为对应 block_type。
- 每个簇若含 ≥2 个对象，进入 VLM 类型识别。

**Step 4：VLM 类型识别 + Semantic Captioning（合并原 image captioning）**

- 把候选簇的 bbox 区域从整页渲染图中裁出，送 qwen3.7-plus / gpt-4o 等 VLM：
  - 判断 `block_type`（native_logic_diagram / decorated_diagram / virtual_table / native_table 等）。
  - 生成完整 `semantic_description`（expression_goal / expression_role / expression_features / vlm_caption / teaching_use）。
- **此步骤即替代原 `cli_caption` 的 image captioning**：不再对单图做 caption，而是对聚类后的可视逻辑块做 caption；输出 `<stem>_captions.md` 的条目对象从单图变为单块。
- 对独立 raster/vector/visio 块，可直接继承原 caption（caption.md 内容），减少 VLM 调用。

### 3.5 拓扑结构生成（图/树）

对 `native_logic_diagram`、`flowchart`、`decorated_diagram` 等块：

- **节点**：shape/文本框/像素图子对象 → 提取文本、类型、归一化 bbox。
- **边**：解析 `<p:cxnSp>` 连接符，或通过几何规则推断：
  - 箭头线对象（connector）明确连接两个 shape → 生成 `edge`。
  - 若 connector 缺失，则用空间最近邻启发式：对每个 shape 找其右侧/下侧最近的 shape，生成候选边，阈值过滤。
- **树/图**： edges 成图；若 edges 形成层级结构（如组织结构图），可检测为树（无环 + 一个根）。
- **表格**：`native_table` 用 `<a:tbl>` 直接转 markdown 矩阵；`virtual_table` 用网格聚类（按行/列中心点 k-means）推断行列，再生成 markdown 矩阵。

### 3.6 资源目录组织（对齐原结构 + 新需求）

保留原 `_proc/img/by_page/`、`images/` 等结构，新增/复用：

```
结果目录/
├─ images/                      # 所有可视逻辑块的渲染图（统一目录，仅块级）
│   ├─ slide_12_blk_01.png
│   └─ ...
├─ sources/                     # 所有可视逻辑块的矢量/源文件（统一目录，无矢量块则不生成）
│   ├─ slide_12_blk_01.svg
│   ├─ slide_05_blk_02.vsdx
│   └─ ...
├─ <stem>_visual_blocks.json    # 全课件所有页的可视逻辑块（替换 binding.json）
├─ <stem>_visualBlock_text_binding.json  # 块↔文本图文关联（cross_modal_relations 独立视图）
├─ <stem>_textbook.md           # 教材文案（author 步骤产出）
├─ <stem>_captions.md           # 可视逻辑块级 AI 解读（由 blocks 步骤产出）
├─ <stem>_formulas.md           # 公式清单（formula 步骤产出）
├─ <stem>_texts.md              # 文本清单（text 步骤产出）
├─ state.json / pipeline.log    # 保留断点续传
└─ 过程文件/
    ├─ text/                    # 步骤1 text：texts.md + text_entries.json
    ├─ formula/                 # 步骤2 formula：formulas.md
    └─ blocks/                  # 步骤3 blocks：atomic_objects.json（几何地基）+ by_page/ + images/ + sources/ 工作副本
```

### 3.7 跨模态关系（cross_modal_relations）

沿用并扩展原 `cli_bind` 的 relation 能力：

- 对每个 block，找到页文本中与其 bbox 最近、或语义上最相关的句子/段落作为 `text_anchor`。
- 调用 DeepSeek / Qwen 判断 `relation_type`：
  - `title_caption`：标题定义块主题
  - `elaboration`：文本展开说明块
  - `example`：块是文本的示例
  - `summary`：块总结文本
  - `definition`：块定义文本中的概念
  - `contrast`：块与文本形成对比
- 生成 ≤60 字 `semantic_link`。

### 3.8 新处理流程（`pptx-paser`）

```
原始 pptx
  ↓
1. text     —— 逐页提取文本框/表格文本（排除页眉页脚）
  ↓
2. formula  —— 提取公式为 LaTeX
  ↓
3. img      —— 提取像素图/矢量图/Visio/图表/原生 shape/connector/table，输出 atomic_objects.json
  ↓
4. blocks   —— 空间聚类 → 块渲染图 → VLM 类型识别 + Semantic Captioning → 拓扑生成
              输出：images/、sources/、<stem>_captions.md、by_page/blocks/
  ↓
5. related  —— 按 block 过滤无关内容（logo/装饰/单位名）
  ↓
6. author   —— 文本/公式/块 caption 生成教材文案
  ↓
7. blocks-json —— 组装 <stem>_visual_blocks.json（含 cross_modal_relations）
```

**为什么 `text`/`formula` 提到 `img` 前面？**
- `blocks` 步骤调用 VLM 做 caption 时，需要把该页文本和公式作为上下文一起喂给模型，生成更准确的 `expression_goal`/`vlm_caption`。
- `img` 步骤本身只产生原子对象和本地渲染，不依赖 text/formula；提前 text/formula 不会阻塞 img。

### 3.9 对 11 条用户要求的逐条响应

| # | 用户要求 | 方案实现 |
|---|---------|---------|
| 1 | 空间聚类或 VLM 分割 | 并查集空间聚类 + VLM 类型识别/语义描述 |
| 2 | 生成图/树结构，替换 binding.json | `*_visual_blocks.json`（按用户 schema 扩充），`cli_bind` 改造成 `cli_blocks` 或重定向输出 |
| 3 | 每个块 Semantic Captioning | 在 `blocks` 步骤内调用 VLM，**合并原 image captioning**；`captions.md` 条目对象变为单块 |
| 4 | 块与页面文字关系描述 | `cross_modal_relations` 生成 |
| 5 | JSON 全栈解析（扩充 schema） | 第 2 节 schema |
| 6 | 所有页面合成一个 json | `slides[]` 数组聚合 |
| 7 | 所有块渲染图放同一 image 目录 | `images/` 顶层统一目录 |
| 8 | 所有块资源图放同一 source 目录 | `sources/` 顶层统一目录 |
| 9 | 保留文本/公式/扩写步骤 | 原 `pptx-text`、`pptx-formula`、`pptx-author` 不变；`pptx-paser` 流程中 `text`/`formula` 提前到 `img` 之前 |
| 10 | 尽量不改变原处理流程 | `pptx-paser` 流程改为 `text → formula → img → blocks → related → author → blocks-json`；`caption` 步骤并入 `blocks` |
| 11 | 尽量不改变原输出文件结构 | 新增 `*_visual_blocks.json` 替代 `*_binding.json`，其余目录/文件保留 |

---

## 4. 代码改造实施计划

### 4.1 新增文件

| 文件 | 职责 |
|-----|------|
| `src/pptx_wzq/visual_blocks.py` | 核心：原子对象扩展、空间聚类、块类型识别、拓扑生成、渲染、semantic captioning、跨模态关系 |
| `src/pptx_wzq/cli_blocks.py` | CLI 入口 `pptx-blocks`，替代/扩展 `pptx-bind`，输出 `*_visual_blocks.json` |
| `tests/test_visual_blocks.py` | 单元测试（合成数据 + 真实课件一页） |

### 4.2 修改文件

| 文件 | 修改点 |
|-----|--------|
| `extract_pptx_images.py` | 1. 新增 `iter_native_shapes()`、`iter_connectors()`、`iter_tables()`，把 shape/connector/table 也作为原子对象输出；2. `extract()` 返回 records + atomic_objects；3. manifest 增加 shape/connector/table 列 |
| `cli_img.py` | 扩展原子对象输出 shape/connector/table；提取后输出 `atomic_objects.json` 供 blocks 步骤使用；**不直接调用 VLM caption** |
| `cli_blocks.py`（新增） | 核心新步骤：读 atomic_objects + text/formula → 空间聚类 → 渲染块 → VLM 类型识别 + Semantic Captioning → 拓扑生成 → 输出 `<stem>_captions.md`、`<stem>_visual_blocks.json` |
| `cli_caption.py` | **功能并入 `cli_blocks`**：原命令保留为空壳/兼容模式，避免下游脚本报错 |
| `cli_related.py` | 相关性过滤改为按 block 过滤 |
| `cli_paser.py` | 1. STEPS 改为 `text → formula → blocks → related → author → blocks_json`（6 步，原 `img` 步骤已并入 blocks 自举）；2. `_organize()` 把 blocks 渲染图/资源图归位到 `images/`、`sources/` 并导出 `<stem>_visualBlock_text_binding.json`；3. 输出 `<stem>_visual_blocks.json`；4. state.json STEPS 同步更新 |
| `pyproject.toml` | 新增 console_scripts：`pptx-blocks`；版本 bump 到 1.5.0 |

### 4.3 最小破坏点

- **不删除** `pptx-bind` 命令，新增 `pptx-blocks` 命令，两者可并存 1~2 个版本。
- **`cli_img.py` 默认行为不变**：新增 `--visual-blocks` 开关；`pptx-paser` 整体编排默认启用可视逻辑块。
- **`images/` 目录语义**：现为"块渲染图"统一目录，文件名规则 `slide_NN_blk_NN.png`；原子图片概念已取消，矢量/Visio 源归 `sources/`。

---

## 5. 关键算法细节

### 5.1 空间聚类参数

```python
CLUSTER_CONFIG = {
    "min_area": 1600,          # 40x40 px，小于此视为噪声
    "alpha_h": 1.5,            # 水平方向邻近系数
    "alpha_v": 1.5,            # 垂直方向邻近系数
    "max_gap_px": 60,          # 绝对最大间距，防止远距离误连
    "max_blocks_per_slide": 6, # 一页最多输出 6 个块，超限时按面积/语义重要性合并
}
```

### 5.2 渲染策略

- **首选**：LibreOffice 整页渲染 → 按 block bbox 裁剪（已有 `render_pptx_pages` + `crop_page_png` 可复用）。
- **备选**：python-pptx 打开 pptx，复制 slide 上属于该 block 的 shape 到新 slide，删除其他 shape，然后 export PNG（精度高但慢）。
- **矢量块**：同时导出 SVG（通过 LibreOffice `soffice --headless --convert-to svg` 或 python-pptx 选 shape 后自己拼 SVG）。

### 5.3 VLM 调用策略（Token 控制）

- 一页最多 6 个 block，每个块 1 次 VLM 类型识别/语义描述 ≈ 6 次调用/页。
- 原 image captioning 的 `qwen3.7-plus` 可直接复用（输入为 block 裁剪图 + 页文本上下文）。
- 跨模态关系调用 DeepSeek，每块 1 次。
- 独立 raster/vector/visio 块优先复用原 caption，不重复调用 VLM。

### 5.4 表格检测

- **native_table**：解析 `<a:tbl>` 的 `<a:tr>` / `<a:tc>`，直接生成 markdown 矩阵。
- **virtual_table**：对 shape/文本框集合按行/列中心点做 1D k-means，推断行列数；单元格文本取对应位置 shape 文本；生成 markdown 矩阵。

### 5.5 图/树拓扑

- 节点 = 块内非 connector 原子对象。
- 边来源优先级：
  1. `<p:cxnSp>` 的 `spid` 指向（startShapeId / endShapeId）。
  2. connector 几何中心落在某 shape bbox 内。
  3. 空间最近邻启发式（右/下优先，距离阈值）。
- 若 edges 无环且只有一个入度为 0 的节点 → `topology_type = "tree"`，否则 `"directed_graph"`。

---

## 6. 工作提示词（可直接交给智能体执行）

```markdown
# 角色
你是 Python PPTX 解析专家、高等教育内容工程专家、多模态 RAG 系统设计师。

# 任务
将 pptx-kb 项目从"单图/单公式/单图表"提取升级为"可视逻辑块（Visual Block）"全栈解析，输出符合 `pptx_multimodal_slide_v2.0` schema 的 JSON，替换现有 `*_binding.json`。

# 输入
- 项目目录：`C:/Users/wuzhe/WorkBuddy/2026-08-02-14-20-14/pptx-kb`
- 当前代码：`src/pptx_wzq/extract_pptx_images.py`（图片/公式/图表提取）、`cli_img.py`、`cli_bind.py`、`cli_paser.py` 等
- 目标 schema：见 `docs/交付物体系结构.md`（word-wzq 项目）及用户提供的 JSON 示例

# 必须满足的 11 条要求
1. 利用空间聚类或 VLM（或其他方法）实现每页可视逻辑块的分割和类型识别；一页通常 1~6 个块。
2. 生成每个可视逻辑块的图结构或树结构，按页面组织成 JSON，替换原 `*_binding.json`；输出文件名为 `<stem>_visual_blocks.json`。
3. 对每个可视逻辑块进行 Semantic Captioning（包括像素图），替代之前的 image captioning；输出 `<stem>_captions.md` 的 caption 对象改为块级别。
4. 在每个页面的 JSON 中生成可视逻辑块与页面文字的关系描述（cross_modal_relations）。
5. JSON 必须包含整页信息、每个块的几何/拓扑/资源/内容/类型/抽象表达特征/教学语义等全栈解析；schema 按本文档第 2 节实现。
6. 所有页面的 JSON 内容合成一个 json 文件（`slides[]` 数组）。
7. 所有页面的可视逻辑块渲染图放在同一 `images/` 目录下，命名规则 `slide_NN_blk_NN.png`。
8. 所有页面的可视逻辑块资源图（svg/vsdx 等）放在同一 `sources/` 目录下。
9. 保留页面文本提取、公式提取、文本扩写等原有步骤，不得改动其核心逻辑。
10. 尽量不改变原先的处理流程：`pptx-paser` 流程改为 `text → formula → img → blocks → related → author → blocks-json`；原 `caption` 步骤并入 `blocks`，`cli_bind` 升级为 `cli_blocks`/`cli_blocks_json`。
11. 尽量不改变原先的输出文件结构：除新增 `*_visual_blocks.json` 和把 `images/` 语义扩展为块图统一目录外，其余目录/文件保留。

# 具体步骤
1. 新增 `src/pptx_wzq/visual_blocks.py`：
   - 定义 `AtomicObject` dataclass，包含 page/obj_id/kind/bbox/z_index/text/source_media/output_file/original_format/children。
   - 实现 `extract_blocks(pptx_path, out_dir, atomic_objects, page_texts, client, model)`：
     * 过滤噪声（面积 <1600、背景、页眉页脚、logo）。
     * 用并查集按 bbox 邻近规则聚类（alpha_h=alpha_v=1.5，max_gap_px=60）。
     * 单对象簇直接映射为 raster_image/vector_image/visio_diagram/chart/formula_block；多对象簇调用 VLM 判定 block_type（native_logic_diagram/decorated_diagram/native_table/virtual_table）。
     * 对每个块生成 internal_structure：native_table/virtual_table → markdown 矩阵；logic diagram → nodes + edges（优先解析 cxnSp，缺失则用空间最近邻）。
     * 调用 VLM 生成 semantic_description（expression_goal/role/features/vlm_caption/teaching_use）。
   - 实现 `render_block_to_png()` 和 `export_block_vector()`，生成块级渲染图和资源文件。
   - 实现 `build_cross_modal_relations()`，生成 text_anchor ↔ block 关系。

2. 扩展 `src/pptx_wzq/extract_pptx_images.py`：
   - 新增 `iter_native_shapes()`、`iter_connectors()`、`iter_tables()`，在 `extract()` 主循环中把 shape/connector/table 也作为原子对象输出。
   - `extract()` 返回 `records` 和 `atomic_objects` 两个字典/列表。
   - manifest.json 增加 shape/connector/table 类型列。

3. 修改 `src/pptx_wzq/cli_img.py`：
   - 扩展原子对象输出 shape/connector/table；`extract()` 返回 records + `atomic_objects.json`。
   - **不直接调用 VLM caption**，只做本地提取与原子对象归档。
   - 新增 `--no-atomic-objects` 开关用于调试；默认输出 `atomic_objects.json`。

4. **新增 `src/pptx_wzq/cli_blocks.py`（核心新步骤，替代原 `cli_caption` + `cli_bind` 的caption/绑定能力）**：
   - 输入：`atomic_objects.json`、`<stem>_texts.md`、`<stem>_formulas.md`、原始 pptx（用于整页渲染）。
   - 流程：
     * 读取 atomic_objects，按页分组；
     * 空间聚类 → Visual Block 候选；
     * 对每个块渲染 PNG（整页渲染 + 裁剪，或 python-pptx 选 shape 导出）；
     * 调用 VLM（qwen3.7-plus）生成 `block_type` + `semantic_description`；
     * 生成拓扑结构（图/树/表格矩阵）；
     * 写出 `<stem>_captions.md`（条目对象变为单块，如 `### IMG0001 — slide_12_blk_01`）；
     * 写出 `<stem>_visual_blocks.json`（或作为中间产物供 `blocks-json` 最终合并）。
   - 支持 `--resume` 断点续跑。

5. 修改 `src/pptx_wzq/cli_caption.py`：
   - **功能并入 `cli_blocks`**；原命令保留为空壳兼容模式，避免下游脚本报错。

6. 修改 `src/pptx_wzq/cli_related.py`：
   - 相关性过滤按 block 粒度执行，剔除与教学主题无关的 block（logo/装饰/单位名）。

7. 新增/修改 `src/pptx_wzq/cli_blocks_json.py`（CLI 入口 `pptx-blocks-json`，可选，负责最终 JSON 合并）：
   - 读取 textbook.md、`<stem>_captions.md`、blocks 中间产物、texts.md。
   - 组装 `<stem>_visual_blocks.json`，包含 slides[]、每页 textual_content、visual_blocks[]、cross_modal_relations[]、summary。
   - 支持 `--resume` 断点续跑。
   - *注：若 `cli_blocks.py` 已直接输出完整 JSON，则此步骤可省略，由 `cli_blocks.py` 兼任。*

8. 修改 `src/pptx_wzq/cli_paser.py`：
   - STEPS 列表改为 `text → formula → img → blocks → related → author → blocks-json`。
   - 删除/合并原 `caption` 步骤到 `blocks`。
   - `_organize()` 把块渲染图/资源图从 `_proc/img/blocks/` 归位到顶层 `images/`、`sources/`。
   - 输出文件从 `<stem>_binding.json` 改为 `<stem>_visual_blocks.json`。
   - 最终报告更新说明。

9. 修改 `pyproject.toml`：
   - 新增 `pptx-blocks = "pptx_wzq.cli_blocks:main"`（必要时再加 `pptx-blocks-json`）。
   - 版本 bump 到 1.5.0。

# 输出产物验证清单
- `pptx-blocks <目录>` 能成功生成 `<stem>_visual_blocks.json`。
- JSON 顶层必须有 `$schema`、`stem`、`tool_version`、`generated_at`、`slides[]`、`summary`。
- 每个 slide 必须有 `slide_info`、`textual_content`、`visual_blocks[]`、`cross_modal_relations[]`。
- 每个 visual_block 必须有 `block_id`、`block_type`、`bbox`、`assets`、`internal_structure`、`semantic_description`。
- `images/` 下存在 `slide_NN_blk_NN.png`；`sources/` 下存在对应矢量/源文件。
- 原有 `textbook.md`、`formulas.md`、`texts.md`、`state.json`、`pipeline.log` 仍存在且内容正确。

# 注意事项
- 不要删除 `pptx-bind` 命令，先新增 `pptx-blocks`，保留向后兼容。
- 空间聚类参数可配置，默认避免远距离误连。
- VLM 调用控制：一页最多 6 块，独立 raster/vector 块尽量复用原 caption 不重复调用。
- 所有代码必须通过语法检查；关键函数需写单测。
```

---

## 7. 风险与建议

| 风险 | 影响 | 缓解 |
|-----|------|------|
| 原生 shape/connector 解析复杂，表格与逻辑图误判 | 块类型不准 | 先用规则 + VLM 投票；virtual_table 与 native_table 可并存标签 |
| 一页对象过多导致聚类碎片化 | 输出块 >6 | 加 `max_blocks_per_slide`，小噪声块合并到最近大块或丢弃 |
| VLM 调用量增加 | Token 成本上升 | 独立单图复用 caption；复合块才调 VLM |
| 渲染整页再裁剪速度慢 | 大课件耗时 | 使用整页渲染缓存；只对含复合块的页渲染 |
| 现有下游消费 binding.json | 兼容性问题 | 保留 `pptx-bind` 旧命令；新 `pptx-blocks` 输出新 schema |

---

## 8. 下一步建议

1. 先实现 `visual_blocks.py` 的骨架 + 空间聚类（无需 VLM，用合成 PPT 数据验证聚类）。
2. 再接入 VLM 做类型识别 + Semantic Captioning（用 1~2 页真实课件验证）。
3. 改造 `cli_blocks.py` 把 caption + 块分析合并为一步。
4. 最后改造 `cli_paser` 流程为 `text → formula → img → blocks → related → author → blocks-json`，输出完整 JSON 并 bump 1.5.0 发布。
